VISION_PROVIDER=mock
REVIEW_SCORE_THRESHOLD=0.75
MINIMAX_MODEL=MiniMax-M2.1
# Keep secret only in local .env (never commit real keys)
MINIMAX_API_KEY=

# Optional external provider config (placeholder)
# VISION_API_URL=
# VISION_API_KEY=
