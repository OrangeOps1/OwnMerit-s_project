# Frontend Workspace

Place your React app in this folder.

## Recommended quick setup (Vite)

```bash
cd frontend
npm create vite@latest . -- --template react
npm install
copy .env.example .env
npm run dev
```

## Environment

- `NEXT_PUBLIC_API_BASE_URL` points to FastAPI backend
